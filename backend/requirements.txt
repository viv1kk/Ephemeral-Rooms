# Runtime dependencies, pinned exactly.
#
# Two of these are load-bearing and must not be moved without re-running the
# cross-language compatibility test (spec section 0.1): pycrdt and
# pycrdt-websocket must stay in step with the browser's yjs, y-protocols and
# y-codemirror.next. The verified quintet is recorded in the README.
#
# Note there is deliberately no `python-multipart`: uploads arrive as a raw PUT
# body read through request.stream(), never as multipart, because FastAPI's
# multipart handling would spool every chunk to a second temp file outside the
# reservation ledger's accounting (spec section 15).

fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
aiofiles==25.1.0

# CRDT. pycrdt ships as a compiled Rust extension, so confirm a manylinux wheel
# exists for the target Python version and CPU architecture before deploying;
# otherwise pip will attempt a source build and require a Rust toolchain.
pycrdt==0.14.4
pycrdt-websocket==0.16.4

# Transitives pinned so the install is reproducible.
annotated-types==0.8.0
anyio==4.15.1
click==8.5.0
h11==0.16.0
httptools==0.8.0
idna==3.19
pydantic_core==2.46.5
python-dotenv==1.2.3
PyYAML==6.0.3
annotated-doc==0.0.5
pycrdt-store==0.1.5
sqlite-anyio==0.2.10
sniffio==1.3.1
typing_extensions==4.16.0
typing-inspection==0.4.4
watchfiles==1.2.0
websockets==17.1
