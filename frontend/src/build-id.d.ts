// Replaced at build time by the `define` block in vite.config.ts. Declared
// rather than imported because it is a literal substituted into the bundle,
// not a module - there is nothing to import at runtime.
declare const __BUILD_ID__: string;
